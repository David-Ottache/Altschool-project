import React from 'react'

export const About = () => {
  return (
    <div>
      <h1>About the Exams</h1>
      <p>Setup react-router, implement Nested routes, 404 pages and error boundary. 
        Set up fake userAuthContext using the context API to always carry out a fake Authentication, bonus -
        extract out a custom hook to get the currently logged in user, implement SEO and Navigation menu that will show on each page.
      </p>
      
      
      

    </div>
  );
}

export default About;